<!doctype html>
<html lang="en">
<%- include widgets/header.ejs %>
<link rel="stylesheet" href="css/loader.css"/>
<%- include widgets/mainNav.ejs %>
<div class="container py-8 my-8">
  <h1><%= course.title %></h1>
    <div class="my-5">
    <% if(user && (user.permissions.admin || user.permissions.classes)){ %>
      <div class="card">
          <div class="card-body">
              <tr>

                    <td>Emails:</td>
                <% console.log(course.students); 
                course.students.forEach(function(user) { %>
                    <tr>
                        <%= user.email %>,
                     </tr>
                  <% }); %>
              </tr>
              <div class="col-md-8 mx-auto px-4 py-5 my-3">
                  <p><b>Instructor:</b> <%= course.instructor %><p>
                  <p><b>Location:</b> <%= course.location %><p>
                  <p><b>Dates:</b> <%= course.startDate %> - <%= course.endDate%><p>
                  <p><b>Time:</b> <%= course.classTime %><p>
                  <p><b>Day of the Week:</b> <%= course.day %><p>
                  <p><b>Enroll by:</b> <%= course.deadline %><p>
                  <p><b>Spots Left:</b> <%= course.openSpots %> / <%=course.totalSpots%> <p>
                  <p><%= course.details %></p>
              </div>
          </div>
      </div>
        <table class="table table-striped">
            <thead class="sermon">
                <tr>
                    <td>Name</td>
                    <td>Graduation Year</td>
                    <td>Major</td>
                    <td>Email</td>
                    <td>Phone Number</td>
                </tr>
            </thead>
            <tbody>
                <% course.students.forEach(function(user) { %>
                    <tr>
                        <td>
                            <%= user.name %>
                        </td>
                        <td>
                            <%= user.grad %>
                        </td>
                        <td>
                          <%= user.major %>
                        </td>
                        <td>
                          <%= user.email %>
                        </td>
                        <td>
                          <%= user.phoneNumber %>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
    <% } %>
</div>
<%- include widgets/footer.ejs %>
</html>
